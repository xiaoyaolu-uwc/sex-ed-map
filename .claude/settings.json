{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && r=$(git -C \"$(dirname \"$f\")\" rev-parse --show-toplevel 2>/dev/null) && git -C \"$r\" add \"$f\" && (git -C \"$r\" diff --cached --quiet || git -C \"$r\" commit -m \"auto: update $(basename \\\"$f\\\")\"); } 2>/dev/null || true",
            "statusMessage": "Auto-committing changes..."
          }
        ]
      }
    ]
  }
}
